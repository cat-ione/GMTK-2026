from math import sqrt, sin, cos, tan, asin, acos, atan, atan2, degrees, \
    radians, pi, e, log, log10, exp, ceil, floor, trunc, fmod, modf, pow, \
        hypot, factorial, isnan, isinf, isfinite, copysign, erf, erfc, gamma, \
            lgamma, fsum, gcd, isqrt, comb, perm, prod, inf, nan, tau
from random import random, randint, uniform, choice, shuffle, seed, randrange, \
    sample, triangular, betavariate, expovariate, gammavariate, gauss, \
        lognormvariate, normalvariate, vonmisesvariate, paretovariate, \
            weibullvariate, getrandbits, randbytes
from typing import Any, cast, Never, Callable, TypedDict, Protocol, \
    TYPE_CHECKING, override, Literal, TypeVar, Self, Generator
from os import system, name, chdir, getcwd, listdir, mkdir, remove, rename, \
    rmdir, walk
from functools import lru_cache, cache, cached_property
from enum import Enum, auto
import pygame
import time
